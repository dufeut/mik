// Script with infinite loop (should be interrupted)
export default function(input) {
    while(true) {
        // Infinite loop - should be interrupted by timeout
    }
    return { never: "reached" };
}
